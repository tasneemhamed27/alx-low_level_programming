#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 *main - Entry point 
 *
 *Return: Always o (success)
 */  
int main(void)
{
char ch = 'a';
char CH = 'A';
while (ch = 'z')
{
putchar(ch);
ch++;
}
while (ch = 'z')
{
putchar(CH);
ch++;
}
putchar('\n');
return (0);
}
